import React from 'react'
const roles = ''
export const AppAuthContext = React.createContext(roles)
export const ProductionAuthContext = React.createContext(roles)
export const SpaceAuthContext = React.createContext(roles)
export const TheaterAuthContext = React.createContext(roles);
export const UserAuthContext = React.createContext(roles);
